import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },

  conteudo: {
    alignItems: "center",
    justifyContent: "center",
  },

  name: {
    textAlign: "center",
    fontSize: 30,
  },

  image: {
    width: 500,
    height: 500,
  },

  adress: {
    textAlign: "center",
    fontSize: 25,
    color: "#add8e6",
  },

  description: {
    textAlign: "center",
    fontSize: 20,
    color: "#a9a9a9",
  },
});
